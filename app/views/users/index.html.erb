<h1> Browse through the list of Users to meet some new Nakama</h1>
<ul>
    <% @users.map do |user_var|%>
        <li> <%= link_to user_var.username, user_path(user_var.id) %> </li>
    <%end%>

</ul>