<h1> Update a Post </h1>

<%= form_for @post do |p| %>
    <%= p.label :title %>
    <%= p.text_field :title %>

    <%= p.label :content %>
    <%= p.text_field :content %>

    
    <%= p.submit "update your post!" %>
<% end %>